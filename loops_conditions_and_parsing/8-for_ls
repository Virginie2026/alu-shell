#!/usr/bin/env bash
# This script lists directory contents displaying only the part after the first dash

shopt -s nullglob

for file in *
do
    if [ -f "$file" ]
    then
        echo "$file" | cut -d '-' -f 2-
    fi
done
